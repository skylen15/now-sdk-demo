# References Guide

- Treat `sn_schema_dev282837_1780629501956.json` as the read-only single source of truth for the `dev282837` instance schema available through the Now SDK auth alias `pdi-qkb-o2`.
- Use the schema snapshot before querying the instance for table, field, and relationship facts.
- Do not edit, regenerate, or replace schema reference exports unless the user explicitly asks for a schema refresh.
