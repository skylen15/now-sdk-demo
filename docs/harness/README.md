# Spec Harness

This harness keeps story state, implementation workflow, review, and verification evidence consistent.

Run `npm run harness:init` at the start of story work. Replace the starter story and placeholders with project-specific requirements.
