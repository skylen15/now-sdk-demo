{
  "name": "personal-todo",
  "version": "0.0.1",
  "description": "",
  "license": "UNLICENSED",
  "imports": {
    "#now:*": "./@types/servicenow/fluent/*/index.js"
  },
  "scripts": {
    "generate-routes": "tsr generate",
    "watch-routes": "tsr watch",
    "harness:init": "node scripts/harness-init.mjs",
    "harness:validate": "node scripts/harness-validate.mjs",
    "harness:init:build": "node scripts/harness-init.mjs --build",
    "harness:apply": "node scripts/apply-harness.mjs",
    "test:due-dates": "node --experimental-strip-types --test scripts/due-dates.test.mjs",
    "test:reminders": "node --experimental-strip-types --test scripts/reminders.test.mjs",
    "test:installed-page": "playwright test tests/installed-page.spec.ts",
    "build": "npm run generate-routes && now-sdk build",
    "deploy": "now-sdk install",
    "transform": "now-sdk transform",
    "types": "now-sdk dependencies"
  },
  "dependencies": {
    "@kobidev/now-sdk-mock": "^2.0.1",
    "@servicenow/react-components": "^0.1.0",
    "@tanstack/react-query": "^5.101.0",
    "@tanstack/react-router": "^1.170.11",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@servicenow/glide": "27.0.5",
    "@servicenow/sdk": "4.7.0",
    "@tanstack/router-cli": "^1.167.13",
    "@types/react": "18.3.12",
    "@types/react-dom": "18.3.1"
  }
}
